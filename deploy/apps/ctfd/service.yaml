apiVersion: v1
kind: Service
metadata:
  name: ctfd
  namespace: ctfd
  labels:
    app: ctfd          # matched by the VMServiceScrape (anti-cheat metrics)
spec:
  selector:
    app: ctfd
  ports:
    - name: http
      port: 8000
      targetPort: 8000
