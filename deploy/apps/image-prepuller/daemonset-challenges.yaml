# GENERATED by generate-challenge-images.sh — do not hand-edit.
#
# Per-challenge SERVICE + WORKSPACE images actually hosted on registry.alfa.ctf.school,
# re-derived from the cluster's own LabSpace/LabService objects each time a
# challenge is added/changed (see the script for exactly how). These need a pull
# secret (registry.alfa.ctf.school requires htpasswd auth even for pulls) — see
# registry-pull.secret.example.yaml and the README for how that's supplied.
#
# Same "sleep infinity, never run the real entrypoint" pattern as
# daemonset-platform.yaml — see that file for why.
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: image-prepuller-challenges
  namespace: image-prepuller
  labels: { app: image-prepuller, tier: challenges }
spec:
  selector:
    matchLabels: { app: image-prepuller, tier: challenges }
  template:
    metadata:
      labels: { app: image-prepuller, tier: challenges }
    spec:
      tolerations:
        - operator: Exists
      automountServiceAccountToken: false
      imagePullSecrets:
        - name: registry-pull
      securityContext:
        seccompProfile: { type: RuntimeDefault }
      containers:
        - name: "img-1-agentic"
          image: "registry.alfa.ctf.school/agentic:latest"
          imagePullPolicy: IfNotPresent
          command: ["sleep", "infinity"]
          resources:
            requests: { cpu: 5m, memory: 16Mi }
            limits: { cpu: 20m, memory: 32Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 65534
            capabilities: { drop: ["ALL"] }
        - name: "img-2-aiman"
          image: "registry.alfa.ctf.school/aiman:latest"
          imagePullPolicy: IfNotPresent
          command: ["sleep", "infinity"]
          resources:
            requests: { cpu: 5m, memory: 16Mi }
            limits: { cpu: 20m, memory: 32Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 65534
            capabilities: { drop: ["ALL"] }
        - name: "img-3-ats"
          image: "registry.alfa.ctf.school/ats:latest"
          imagePullPolicy: IfNotPresent
          command: ["sleep", "infinity"]
          resources:
            requests: { cpu: 5m, memory: 16Mi }
            limits: { cpu: 20m, memory: 32Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 65534
            capabilities: { drop: ["ALL"] }
        - name: "img-4-avatarius"
          image: "registry.alfa.ctf.school/avatarius:latest"
          imagePullPolicy: IfNotPresent
          command: ["sleep", "infinity"]
          resources:
            requests: { cpu: 5m, memory: 16Mi }
            limits: { cpu: 20m, memory: 32Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 65534
            capabilities: { drop: ["ALL"] }
        - name: "img-5-cerberus-helpdesk"
          image: "registry.alfa.ctf.school/cerberus-helpdesk:latest"
          imagePullPolicy: IfNotPresent
          command: ["sleep", "infinity"]
          resources:
            requests: { cpu: 5m, memory: 16Mi }
            limits: { cpu: 20m, memory: 32Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 65534
            capabilities: { drop: ["ALL"] }
        - name: "img-6-chatbot"
          image: "registry.alfa.ctf.school/chatbot:latest"
          imagePullPolicy: IfNotPresent
          command: ["sleep", "infinity"]
          resources:
            requests: { cpu: 5m, memory: 16Mi }
            limits: { cpu: 20m, memory: 32Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 65534
            capabilities: { drop: ["ALL"] }
        - name: "img-7-consultant"
          image: "registry.alfa.ctf.school/consultant:latest"
          imagePullPolicy: IfNotPresent
          command: ["sleep", "infinity"]
          resources:
            requests: { cpu: 5m, memory: 16Mi }
            limits: { cpu: 20m, memory: 32Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 65534
            capabilities: { drop: ["ALL"] }
        - name: "img-8-find-chat"
          image: "registry.alfa.ctf.school/find_chat:latest"
          imagePullPolicy: IfNotPresent
          command: ["sleep", "infinity"]
          resources:
            requests: { cpu: 5m, memory: 16Mi }
            limits: { cpu: 20m, memory: 32Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 65534
            capabilities: { drop: ["ALL"] }
        - name: "img-9-ghost-in-the-shell-workspace"
          image: "registry.alfa.ctf.school/ghost_in_the_shell-workspace:sha-831030d94014"
          imagePullPolicy: IfNotPresent
          command: ["sleep", "infinity"]
          resources:
            requests: { cpu: 5m, memory: 16Mi }
            limits: { cpu: 20m, memory: 32Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 65534
            capabilities: { drop: ["ALL"] }
        - name: "img-10-ghost-in-the-shell"
          image: "registry.alfa.ctf.school/ghost_in_the_shell:latest"
          imagePullPolicy: IfNotPresent
          command: ["sleep", "infinity"]
          resources:
            requests: { cpu: 5m, memory: 16Mi }
            limits: { cpu: 20m, memory: 32Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 65534
            capabilities: { drop: ["ALL"] }
        - name: "img-11-image-detectors-workspace"
          image: "registry.alfa.ctf.school/image_detectors-workspace:sha-febb5f2e469c"
          imagePullPolicy: IfNotPresent
          command: ["sleep", "infinity"]
          resources:
            requests: { cpu: 5m, memory: 16Mi }
            limits: { cpu: 20m, memory: 32Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 65534
            capabilities: { drop: ["ALL"] }
        - name: "img-12-image-detectors"
          image: "registry.alfa.ctf.school/image_detectors:latest"
          imagePullPolicy: IfNotPresent
          command: ["sleep", "infinity"]
          resources:
            requests: { cpu: 5m, memory: 16Mi }
            limits: { cpu: 20m, memory: 32Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 65534
            capabilities: { drop: ["ALL"] }
        - name: "img-13-members"
          image: "registry.alfa.ctf.school/members:latest"
          imagePullPolicy: IfNotPresent
          command: ["sleep", "infinity"]
          resources:
            requests: { cpu: 5m, memory: 16Mi }
            limits: { cpu: 20m, memory: 32Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 65534
            capabilities: { drop: ["ALL"] }
        - name: "img-14-new-yorker"
          image: "registry.alfa.ctf.school/new_yorker:latest"
          imagePullPolicy: IfNotPresent
          command: ["sleep", "infinity"]
          resources:
            requests: { cpu: 5m, memory: 16Mi }
            limits: { cpu: 20m, memory: 32Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 65534
            capabilities: { drop: ["ALL"] }
        - name: "img-15-ptserial-workspace"
          image: "registry.alfa.ctf.school/ptserial-workspace:sha-cc2ebfa09b58"
          imagePullPolicy: IfNotPresent
          command: ["sleep", "infinity"]
          resources:
            requests: { cpu: 5m, memory: 16Mi }
            limits: { cpu: 20m, memory: 32Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 65534
            capabilities: { drop: ["ALL"] }
        - name: "img-16-ptserial"
          image: "registry.alfa.ctf.school/ptserial:latest"
          imagePullPolicy: IfNotPresent
          command: ["sleep", "infinity"]
          resources:
            requests: { cpu: 5m, memory: 16Mi }
            limits: { cpu: 20m, memory: 32Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 65534
            capabilities: { drop: ["ALL"] }
        - name: "img-17-puzzle-workspace"
          image: "registry.alfa.ctf.school/puzzle-workspace:sha-de70533f97f1"
          imagePullPolicy: IfNotPresent
          command: ["sleep", "infinity"]
          resources:
            requests: { cpu: 5m, memory: 16Mi }
            limits: { cpu: 20m, memory: 32Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 65534
            capabilities: { drop: ["ALL"] }
        - name: "img-18-puzzle"
          image: "registry.alfa.ctf.school/puzzle:latest"
          imagePullPolicy: IfNotPresent
          command: ["sleep", "infinity"]
          resources:
            requests: { cpu: 5m, memory: 16Mi }
            limits: { cpu: 20m, memory: 32Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 65534
            capabilities: { drop: ["ALL"] }
        - name: "img-19-sign-me-workspace"
          image: "registry.alfa.ctf.school/sign-me-workspace:sha-e864815224f1"
          imagePullPolicy: IfNotPresent
          command: ["sleep", "infinity"]
          resources:
            requests: { cpu: 5m, memory: 16Mi }
            limits: { cpu: 20m, memory: 32Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 65534
            capabilities: { drop: ["ALL"] }
        - name: "img-20-sign-me"
          image: "registry.alfa.ctf.school/sign-me:latest"
          imagePullPolicy: IfNotPresent
          command: ["sleep", "infinity"]
          resources:
            requests: { cpu: 5m, memory: 16Mi }
            limits: { cpu: 20m, memory: 32Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 65534
            capabilities: { drop: ["ALL"] }
        - name: "img-21-trojanml"
          image: "registry.alfa.ctf.school/trojanml:latest"
          imagePullPolicy: IfNotPresent
          command: ["sleep", "infinity"]
          resources:
            requests: { cpu: 5m, memory: 16Mi }
            limits: { cpu: 20m, memory: 32Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 65534
            capabilities: { drop: ["ALL"] }
