apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - service-headless.yaml
  - service.yaml
  - statefulset.yaml
  - networkpolicy.yaml   # ingress: only CTFd + KeyDB peers (unauthenticated store)
