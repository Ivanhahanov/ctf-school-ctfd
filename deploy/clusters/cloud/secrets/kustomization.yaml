# Cloud secret, reconciled by the `secrets` Flux Kustomization (not the root),
# which decrypts it with SOPS. Applied only after `infrastructure` has created
# the ctfd / controller-system namespaces.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - secrets.enc.yaml
