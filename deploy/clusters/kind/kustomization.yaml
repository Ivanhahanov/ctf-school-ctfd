apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - flux-system.yaml   # the repo source + root Kustomization (self-managed)
  - sources.yaml       # controller + challenges repo sources
  - sync.yaml          # platform Kustomizations
