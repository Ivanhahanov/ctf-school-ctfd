# Dev secret (SOPS-encrypted, same mechanism as cloud), reconciled by the `secrets`
# Flux Kustomization which decrypts it — applied only after `infrastructure` has
# created the ctfd / controller-system namespaces.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - secrets.enc.yaml
