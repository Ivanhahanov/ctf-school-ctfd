# Dev secret, reconciled by the `dev-secrets` Flux Kustomization (not the root),
# so it lands only after `infrastructure` has created the target namespaces.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - secrets.yaml
