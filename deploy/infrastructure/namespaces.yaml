apiVersion: v1
kind: Namespace
metadata: { name: cert-manager }
---
apiVersion: v1
kind: Namespace
metadata: { name: envoy-gateway-system }
---
apiVersion: v1
kind: Namespace
metadata: { name: mariadb-operator }
---
apiVersion: v1
kind: Namespace
metadata: { name: monitoring }
---
# The application namespace (MariaDB, KeyDB, CTFd live here). Created by the
# infrastructure Kustomization so the data/cache/ctfd apps that depend on it have
apiVersion: v1
kind: Namespace
metadata: { name: ctfd }
---
# The controller namespace. Created HERE (not by the controller's own config) so it
# exists before the dev-secrets and the controller Deployment need it — one owner,
# no bootstrap race. The controller Kustomization only deploys INTO it.
apiVersion: v1
kind: Namespace
metadata: { name: controller-system }
